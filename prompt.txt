Fix these two bugs in C:\Users\user\Desktop\frontend:

1. server/routes/orders.js - Error "11 values for 10 columns"
   - Open the file and count the INSERT columns vs values
   - Make sure they match exactly
   - Check the orders table schema in server/db/database.js and match it

2. client/src/context/ThemeContext.jsx
   - Dark/light mode toggle not working
   - Make sure theme is applied as data-theme attribute on document.documentElement
   - Persist theme in localStorage
   - Default to dark mode
   - Make sure ThemeContext is wrapping the app in main.jsx or App.jsx